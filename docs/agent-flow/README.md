# Archived architecture explainer

This two-tab explainer is preserved as historical product-discovery evidence.
It is **superseded** by:

- `../../docs/design.md` for the authoritative solution;
- `../../DESIGN.md` for the interaction design;
- `../genie-ui/index.html` for the current Living Cinema prototype.

Its stage count, agent count, provider inventory, script examples, release
language, and future-feature assumptions must not be used as implementation
requirements.
